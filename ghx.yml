title-lint:
  installation:
    org: true
    repos:
      - base-task
      - title-lint
  repo_settings:
    regex: foo
  org_settings:
    final:
      regex: "[A-Z]+-[0-9]+"
    default:
      foo: bar
