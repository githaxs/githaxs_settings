installations:
  title-lint:
    org: true
    repos:
      - base-task
      - title-lint
    topics:
      - api
      - web
    languages:
      - python
      - go

settings:
  title-lint:
    regex: "[A-Z]+-[0-9]+"

org_settings:
  title-lint:
    final: "[A-Z]+-[0-9]+"
